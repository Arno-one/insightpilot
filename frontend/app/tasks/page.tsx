import { AppShell } from "@/components/layout/AppShell";

export default function TasksPage() {
  return (
    <AppShell>
      <p className="eyebrow">Execution</p>
      <h1>销售任务</h1>
      <p className="lead">销售员在这里处理由主管确认后的跟进任务。</p>
    </AppShell>
  );
}
