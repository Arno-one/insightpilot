# CSV 失败行二次导入测试记录

## 测试日期

- 2026-07-02

## 功能范围

- 导入页失败行可编辑草稿
- 失败行重新生成 CSV
- 失败行二次导入
- 修正后再次复用现有 `/api/crm/import/{entity}` 接口

## 测试方式

### 1. 前端构建检查

执行命令：

```powershell
npm.cmd run build:verify
```

结果：

- 通过
- `/imports` 页面构建成功
- 本次新增失败行编辑与二次导入逻辑未引入 TypeScript 错误

### 2. 后端语法检查

执行命令：

```powershell
python -m compileall backend/app/modules/crm
```

结果：

- 通过

### 3. 二次导入烟测

测试账号：

- `manager / Manager@123456`

测试步骤：

1. 首次导入一条客户数据，确保入库成功
2. 再次导入同一条客户数据，制造“重复主键失败行”
3. 读取失败行原始字段数据
4. 将失败行里的 `customer_id` 修正为新值
5. 用修正后的同一行数据重新生成 CSV
6. 再次调用现有客户导入接口
7. 清理测试数据

结果：

- 首次导入成功：`success_count = 1`
- 第二次重复导入被正确拦截：`failed_count = 1`
- 修正后的失败行再次导入成功：`success_count = 1`
- 修正后没有新增失败：`retry_failed_count = 0`
- 修正后的业务主键已成功写入：`retry_inserted_keys = ["retry_fix_c_1782961794"]`

## 结论

- 本轮“失败行修正后二次导入”功能测试通过
- 当前实现满足预期闭环：
  - 用户可以基于失败行原始数据继续修改
  - 前端可以重新生成标准 CSV
  - 二次导入继续复用现有后端导入接口
  - 不需要新增额外后端重试协议

## 备注

- 本次烟测以接口闭环和前端构建通过为主，未额外执行浏览器端手工点击录屏测试
- 测试过程中生成的数据已在结束后清理
